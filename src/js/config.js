// copied from server

const config = {
    chunkSize: 512,
    boardWidth: 8, // in chunks
    boardHeight: 8,
    palette: [
        [255, 255, 255],
        [0, 0, 0]
    ]
}

module.exports = config